# ============================================
# coin_csms 백엔드 환경 변수 설정 예시
# ============================================
# 사용 방법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 필요한 값들을 수정
# 3. docker-compose.prod.yml에서 사용: docker-compose --env-file .env up -d
# ============================================

# ============================================
# 애플리케이션 환경 설정
# ============================================
APP_ENV=prod

# ============================================
# 데이터베이스 설정 (선택사항 - 환경 변수로 오버라이드)
# ============================================
# 주의: DB 설정은 config.json에서 관리합니다.
# 환경 변수로 오버라이드하려면 아래 변수들을 설정하세요.
# DB_HOST=localhost
# DB_PORT=5432
# DB_DATABASE=coin_system_cloud
# DB_USER=foxya
# DB_PASSWORD=your-db-password

# ============================================
# JWT 설정 (선택사항 - 환경 변수로 오버라이드)
# ============================================
# JWT_SECRET=your-jwt-secret-key-change-this-in-production

# ============================================
# SMTP 설정 (선택사항 - 환경 변수로 오버라이드)
# ============================================
# SMTP_USERNAME=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# ============================================
# Google OAuth 설정 (선택사항 - 환경 변수로 오버라이드)
# ============================================
# Google OAuth는 현재 코드에서 사용되지 않습니다.
# 필요시 아래 변수들을 설정하세요.
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# ============================================
# 암호화 키 (필수)
# ============================================
ENCRYPTION_KEY=your-secret-encryption-key-change-this-in-production

# ============================================
# JVM 옵션
# ============================================
JAVA_OPTS=-Xmx1024m -Xms512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication

# ============================================
# 타임존 설정
# ============================================
TZ=Asia/Seoul

# ============================================
# Docker 이미지 버전
# ============================================
APP_VERSION=latest
